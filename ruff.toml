target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# 運用スクリプトはencoding設定やargparse処理を先に実行してから
# importする構成が意図的なため、E402(import順)は対象外とする
"programing files/*.py" = ["E402"]
